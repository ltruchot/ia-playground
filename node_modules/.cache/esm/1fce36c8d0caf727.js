let flipCross,randomHelper,checkResolvedRiddle;_d0c‍.x([["aiRiddle1",()=>aiRiddle1]]);_d0c‍.w("./../extra/booleanMatrixHelper",[["flipCross",["flipCross"],function(v){flipCross=v}]]);_d0c‍.w("./../core/randomHelper",[["*",null,function(v){randomHelper=v}]]);_d0c‍.w("./testSolution",[["checkResolvedRiddle",["checkResolvedRiddle"],function(v){checkResolvedRiddle=v}]]);// extra




       const aiRiddle1 = () => {
  let bestBigO = Infinity;
  let bestSolution;
  for (let i = 0; i < 10000; i++) {
    // put your code in this function
    let bigO = 0;
    const solution = [];
    const resolveRiddle = matrix => {
      bigO++;
      // case already resolved
      if (checkResolvedRiddle(matrix)) {
        return matrix;
      }

      // random resolution
      const y = randomHelper.pickIndexInArray(matrix);
      const x = randomHelper.pickIndexInArray(matrix[y]);
      solution.push([y, x]);
      return resolveRiddle(flipCross([y, x])(matrix));
    };

    resolveRiddle([[false, false], [false, true]]);

    if (bestBigO > bigO) {
      bestBigO = bigO;
      bestSolution = solution;
    }
  }
  _d0c‍.g.console.log('AI Session results:', bestSolution);
};
